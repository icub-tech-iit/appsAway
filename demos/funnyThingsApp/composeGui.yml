version: "3.7"

x-yarp-base: &yarp-base
  image: icubteamcode/funny-things:master-unstable_master_binaries
  environment:
    - DISPLAY=${DISPLAY}
    - XAUTHORITY=/root/.Xauthority
    - YARP_FORWARD_LOG_ENABLE=1
    - YARP_ROBOT_NAME
  volumes:
    - "/tmp/.X11-unix:/tmp/.X11-unix"
    - "${XAUTHORITY}:/root/.Xauthority"
    - "${HOME}/${YARP_CONF_PATH}:/root/.config/yarp"
    - "/dev:/dev"
    - "${FILE_IMPORT_EXPORT_PATH}:/root/shared-files:rw"
  network_mode: "host"
  privileged: true

x-yarp-speech: &yarp-speech
  image: icubteamcode/speech:master-unstable_master_binaries
  environment:
    - DISPLAY=${DISPLAY}
    - XAUTHORITY=/root/.Xauthority
    - PULSE_SERVER=unix:${XDG_RUNTIME_DIR}/pulse/native
    - YARP_FORWARD_LOG_ENABLE=1
    - YARP_ROBOT_NAME
    - FILE_INPUT
    - LANGUAGE_SYNTHESIS_INPUT
    - VOICE_NAME_INPUT
  volumes:
    - "/tmp/.X11-unix:/tmp/.X11-unix"
    - "${XAUTHORITY}:/root/.Xauthority"
    - "${HOME}/${YARP_CONF_PATH}:/root/.config/yarp"
    - "${FILE_INPUT_PATH}:/root/authorization"
    - "${XDG_RUNTIME_DIR}/pulse/native:${XDG_RUNTIME_DIR}/pulse/native"
    - "~/.config/pulse/cookie:/root/.config/pulse/cookie"
    - "/dev:/dev"
  network_mode: "host"
  privileged: true

services:
  yFunnyThingsApp:
    <<: *yarp-base
    command: funnyThingsApp.AppImage --no-sandbox

  yDemoGoogleSynthesis:
    <<: *yarp-speech
    devices: 
      - "/dev/snd:/dev/snd"
    deploy:
      placement:
        constraints: [node.labels.type != head]
    command: sh -c "googleSynthesis --language ${LANGUAGE_SYNTHESIS_INPUT} --voice ${VOICE_NAME_INPUT} --speed 0.93 --pitch -3.60"

  
