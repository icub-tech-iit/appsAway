//ilaria-carlini
//&<h3>Field changed in app: <ins>Basic Docker Deployment</ins></h3><ul><li>docker_installation_procedure: The docker installation on your machine or cluster of machines is done in an automated way using **Ansible**. Ansible is the simplest way to **automate applications** and IT infrastructures. **Application Deployment + Configuration Management + Continuous Delivery**. The only requirement is that the console machine (your main machine), which will act as a **manager**, must have Ansible installed.\n\nHere is how to install **Ansible** on the various platforms:\n\n#### Ubuntu/Linux:<b>\n\n1. `sudo apt-add-repository -y ppa:ansible/ansible`\n2. `sudo apt-get update`\n3. `sudo apt-get install -y ansible`\n4. `sudo apt-get install openssh-server`\n5. `sudo apt-get install net-tools`\n\n#### MacOS (not supported by all applications):\n\n1. `brew install ansible`\n2. Install docker Desktop from this [link](https://hub.docker.com/editions/community/docker-ce-desktop-mac/)\n3. `brew install ansible`</b></li></ul>
db.steps.update ({_id: ObjectId("5f52487b6de3ecd4c51f19c2")},{$set: {docker_installation_procedure: "The docker installation on your machine or cluster of machines is done in an automated way using **Ansible**. Ansible is the simplest way to **automate applications** and IT infrastructures. **Application Deployment + Configuration Management + Continuous Delivery**. The only requirement is that the console machine (your main machine), which will act as a **manager**, must have Ansible installed.\n\nHere is how to install **Ansible** on the various platforms:\n\n#### Ubuntu/Linux:\n\n1. `sudo apt-add-repository -y ppa:ansible/ansible`\n2. `sudo apt-get update`\n3. `sudo apt-get install -y ansible`\n4. `sudo apt-get install openssh-server`\n5. `sudo apt-get install net-tools`\n\n#### MacOS (not supported by all applications):\n\n1. `brew install ansible`\n2. Install docker Desktop from this [link](https://hub.docker.com/editions/community/docker-ce-desktop-mac/)\n3. `brew install ansible`"}});
